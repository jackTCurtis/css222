#include <iostream>
#include <cmath>
using namespace std;
int age = 17;

int main(){
    if ((age > 16) && (age < 65)){
        cout << "age is within the normal working age." << endl;
    }
    return 0;
}